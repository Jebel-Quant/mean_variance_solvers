# Build the RMT paper ("From Marchenko--Pastur to Woodbury: Direct Solvers for
# Long-Only Mean-Variance Portfolios"). It cites the shared bibliography that
# lives with the minimum-variance paper (../matrix_free/bib/refs.bib) and pulls
# figures/tables from ../experiment via the graphs/ and tables/ symlinks. All
# shared build logic lives in ../common.mk; only the per-paper variables differ.
# Run from this directory, e.g. via the repository-root `make compile`.

DOC := rmt_paper
BIB := ../matrix_free/bib/refs.bib

include ../common.mk
