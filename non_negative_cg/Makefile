# Build the non-negativity paper ("Non-Negative Conjugate Gradients"). It cites
# the shared bibliography that lives with the minimum-variance paper
# (../matrix_free/bib/refs.bib) and pulls figures/tables from ../experiment via
# the graphs/ and tables/ symlinks. All shared build logic lives in
# ../common.mk; only the per-paper variables differ. Run from this directory,
# e.g. via the repository-root `make compile`.

DOC := nncg_paper
BIB := ../matrix_free/bib/refs.bib

include ../common.mk
