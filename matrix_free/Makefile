# Build the minimum-variance paper ("Matrix-Free Methods for Long-Only
# Portfolio Optimization"). Cites bib/refs.bib and pulls figures/tables from
# ../experiment via the graphs/ and tables/ symlinks. All shared build logic
# lives in ../common.mk; only the per-paper variables differ. Run from this
# directory, e.g. via the repository-root `make compile`.

DOC := cg_paper

include ../common.mk
